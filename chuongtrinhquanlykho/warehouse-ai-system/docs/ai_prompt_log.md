# Nhật ký sử dụng AI — Hệ thống quản lý kho

> Đây là minh chứng bắt buộc chấm điểm. Ghi lại **mỗi lần** dùng AI để sinh code,
> thiết kế, tối ưu prompt, hoặc review code.

| Ngày | Giai đoạn | Mục đích | Prompt (rút gọn) | Phản hồi AI (tóm tắt) | Đã kiểm chứng/chỉnh sửa | Người thực hiện |
|---|---|---|---|---|---|---|
| 2026-08-04 | KT2 | Dựng khung + module Auth | "Đọc AGENTS.md, Prompt.md, api_contract.md → dựng cấu trúc thư mục + requirements.txt + seed_data.sql + .env.example + Auth module (bcrypt, JWT, role middleware)" | Tạo 12 file: extensions.py, models/user.py, auth/routes.py, auth/decorators.py, main.py, tests/test_auth.py, requirements.txt, .env.example, seed_data.sql, các placeholder package | Cần kiểm tra bcrypt hash trong seed_data.sql khớp với Password@123 | Sinh viên |

| 2026-08-08 | KT2 | Sinh module Suppliers | "Đọc AGENTS.md mục 8 (error format), Prompt.md mục 6 (bảng suppliers), api_contract.md mục 2 (Suppliers) → kiểm tra file cũ còn dang dở rồi hoàn thiện: models/supplier.py, routers/suppliers.py (5 endpoint, role middleware), tests/test_suppliers.py (19 TC). Ràng buộc: DELETE soft-delete, response lỗi đúng format {error_code, message}" | Phát hiện 3 file đã được AI trước sinh nhưng chưa "nối dây": (1) uncomment import Supplier trong models/__init__.py, (2) đăng ký suppliers_bp trong main.py, (3) comment relationships đến PurchaseOrder/GoodsReceipt/Goods chưa tồn tại để tránh mapper error. Sửa thêm: thêm test_config param vào create_app() để fixture dùng sqlite:///:memory: đúng cách, thêm db.drop_all() teardown | Chạy pytest: **19/19 PASSED** (28.74s). Kiểm chứng soft-delete: DB vẫn còn bản ghi sau DELETE, status='inactive'. Kiểm chứng 403/401/404 error_code đúng format | Sinh viên |

| 2026-08-08 | KT2 | Sinh module Goods | "Đọc Prompt.md mục 6, api_contract.md mục 3 → hoàn thiện models/goods.py, routers/goods.py (5 endpoint), tests/test_goods.py. Ràng buộc: low-stock < min_stock, mở comment relationship Supplier <-> Goods, chuẩn format lỗi, check đủ role middleware." | Tạo mới models/category.py, models/goods.py, routers/goods.py, tests/test_goods.py. Cập nhật main.py và models/__init__.py để đăng ký module. Mở comment preferred_goods trong models/supplier.py. Bổ sung trọn bộ test fixture (tạo user, app, dữ liệu test). | Chạy pytest: **6/6 PASSED** (9.86s). Logic low-stock và phân quyền hoạt động chính xác. | Sinh viên |

| 09/08/2026 | KT2 | Hoàn thiện Module Purchase      
  Orders (PO) | Đọc AGENTS.md, Prompt.md (3.5, 6.1,      
  10), api_contract.md. Yêu cầu: Điền chi tiết API Doc   
  trước. Chốt flow chuyển trạng thái. Code models,       
  routers (có phân quyền), tests. Ràng buộc: quantity    
  > 0, status luồng chuẩn, cập nhật relationship file    
  cũ. Chạy pytest. | 1. Cập nhật api_contract.md. 2.     
  Hỏi người dùng để chốt flow (chờ xác nhận -> đã xác    
  nhận -> đang giao -> đã nhận). 3. Tạo
  models/purchase_order.py. 4. Bỏ comment relationship   
  ở supplier và goods. 5. Tạo routers/purchase_orders.   
  py phân quyền đúng role. 6. Tạo
  tests/test_purchase_orders.py. 7. Chạy 4/4 test        
  cases passed 100%. | Đã kiểm tra luồng API và code     
  pass toàn bộ test. | (Tên của bạn) |

| 12/08/2026 | KT2 | Sinh module Goods Receipts (Phiếu nhập kho) | Đọc api_contract.md mục 5, ai_prompt_log.md, Prompt.md (##6 thiết kế CSDL, ##8 AI). Yêu cầu: Điền chi tiết API trước khi code. Sinh models/goods_receipt.py (GoodsReceipt + GoodsReceiptItem), cập nhật relationships ở supplier/goods/purchase_order. Sinh routers/goods_receipts.py (3 endpoint, phân quyền). Sinh tests/test_goods_receipts.py (22 TC đủ ca đúng + ca lỗi/biên + phân quyền). Ràng buộc: transaction cập nhật tồn kho, unit_price snapshot cố định, quantity > 0, chặn NCC/Goods inactive. | 1. Cập nhật api_contract.md mục 5 (điền đầy đủ chi tiết + error codes, đổi ⬜→✅). 2. Tạo models/goods_receipt.py. 3. Uncomment goods_receipts relationship ở Supplier. 4. Thêm goods_receipt_items relationship ở Goods. 5. Thêm goods_receipts relationship ở PurchaseOrder. 6. Cập nhật models/__init__.py. 7. Tạo routers/goods_receipts.py với transaction logic. 8. Đăng ký blueprint trong main.py. 9. Tạo tests/test_goods_receipts.py 22 TC. | Chạy pytest: **22/22 PASSED** (26.71s). Kiểm chứng: tồn kho cập nhật đúng (+quantity), unit_price lưu snapshot, rollback đúng khi lỗi, phân quyền 403/401 đúng format. | Sinh viên |

| 14/08/2026 | KT2 | Sinh module Goods Issues (Phiếu xuất kho) | Đọc lại api_contract.md mục 6, ai_prompt_log.md, Prompt.md (##6 thiết kế CSDL, ##8 AI). Yêu cầu: Điền chi tiết API Contract trước khi code. Sinh models/goods_issue.py (GoodsIssue + GoodsIssueItem), thêm goods_issue_items relationship vào Goods, cập nhật models/__init__.py. Sinh routers/goods_issues.py (3 endpoint, phân quyền). Sinh tests/test_goods_issues.py (18 TC đủ ca đúng + ca lỗi/biên + phân quyền). Ràng buộc cốt lõi: transaction trừ tồn kho, chặn xuất vượt tồn (INSUFFICIENT_STOCK), không cho tồn kho âm, quantity > 0, chặn Goods inactive. | 1. Cập nhật api_contract.md mục 6 (điền đầy đủ Request/Response/Error codes, đổi ⬜→✅). 2. Tạo models/goods_issue.py (GoodsIssue + GoodsIssueItem — không có unit_price vì phiếu xuất không lưu giá). 3. Thêm goods_issue_items relationship vào models/goods.py. 4. Cập nhật models/__init__.py thêm import GoodsIssue, GoodsIssueItem. 5. Tạo routers/goods_issues.py với logic kiểm tra tổng qty/goods trước khi vào transaction, guard tồn kho âm. 6. Đăng ký blueprint goods_issues_bp trong main.py. 7. Tạo tests/test_goods_issues.py 18 TC (TC01–TC07 ca đúng, TC08–TC16 ca lỗi/biên, TC17–TC18 phân quyền). | Chạy pytest: **18/18 PASSED** (10.79s). Kiểm chứng: tồn kho giảm đúng sau xuất, chặn xuất vượt tồn thành công (quantity_on_hand không thay đổi sau khi bị block), rollback đúng, phân quyền 403/401 đúng format {error_code, message}. | Sinh viên |

| 18/08/2026 | KT2 | Sinh module Stocktakes (Kiểm kê) | Đọc api_contract.md mục 7, ai_prompt_log.md, Prompt.md (##6 thiết kế CSDL, ##8 AI). Yêu cầu: Sinh models/stocktake.py (Stocktake + StocktakeItem), routers/stocktakes.py (3 endpoint: tạo, đề xuất, duyệt). Ràng buộc cốt lõi: khi duyệt phải cập nhật tồn kho bằng difference, chỉ được duyệt bởi warehouse_manager. Viết tests. | 1. Cập nhật api_contract.md mục 7. 2. Tạo models/stocktake.py. 3. Thêm relationship ở Goods. 4. Tạo routers/stocktakes.py. 5. Tạo tests/test_stocktakes.py. | Chạy pytest pass. Kiểm chứng: duyệt phiếu cập nhật tồn kho đúng, phân quyền 403 đúng. | AI (Antigravity) |
| 18/08/2026 | KT2 | Sinh module Supplier Invoices & Payments (Công nợ) | Đọc api_contract.md mục 8, ai_prompt_log.md, Prompt.md. Yêu cầu: Sinh models/supplier_invoice.py và models/supplier_payment.py. Tạo routers xử lý hóa đơn và thanh toán. Ràng buộc: paid_amount tính tổng từ payment, cập nhật trạng thái hóa đơn tự động khi đủ. | 1. Cập nhật api_contract.md mục 8. 2. Tạo models và routers. 3. Viết tests/test_supplier_invoices.py kiểm tra transaction và logic tự đổi trạng thái. | Chạy pytest pass toàn bộ. Logic tổng tiền và tự động đổi payment_status chính xác. | AI (Antigravity) |
| 20/08/2026 | KT2 | Sinh module Reports (Thống kê/Báo cáo) | Đọc lại api_contract.md mục 9, ai_prompt_log.md, Prompt.md (##6 thiết kế CSDL, ##8 kịch bản AI). Chú ý mục 3.7 (yêu cầu chức năng thống kê), mục 4 (hiệu năng — không full-table scan), mục 9 (phân quyền). Yêu cầu: Điền chi tiết API Contract trước khi code. Sinh routers/reports.py (4 endpoint, phân quyền warehouse_manager + admin). Sinh tests/test_reports.py (17 TC). Ràng buộc: avg_cost tính từ goods_receipt_items snapshot, chỉ tính stocktake "đã phê duyệt", query tổng hợp dùng func.sum/count ở tầng DB. | 1. Cập nhật api_contract.md mục 9 (điền chi tiết 4 sub-endpoint: 9.1–9.4, Request/Response/Error codes, đổi ⬜→✅). 2. Tạo routers/reports.py — 4 endpoint: inventory-value (giá trị tồn kho + avg_cost), turnover (vòng quay + slow-moving), top-goods (top nhập/xuất theo qty), stocktake-diff (chênh lệch kiểm kê đã phê duyệt). Helper _parse_date() hỗ trợ YYYY-MM-DD và ISO 8601. 3. Đăng ký reports_bp trong main.py. 4. Tạo tests/test_reports.py (TC01–TC11 ca đúng, TC12–TC15 ca lỗi/biên, TC16–TC17 phân quyền). 5. Cập nhật ai_prompt_log.md. | Chạy pytest: **17/17 PASSED** (36.17s). Kiểm chứng: avg_cost tính đúng (bình quân gia quyền theo lần nhập), turnover_rate = None khi tồn = 0, top-goods xếp hạng đúng, stocktake-diff chỉ lấy phiếu đã duyệt, phân quyền 403/401 đúng format {error_code, message}. | Sinh viên |
| 20/08/2026 | KT3 | Sinh module AI Features (Gợi ý và Báo cáo) | Đọc lại api_contract.md, ai_prompt_log.md và Prompt (chú ý ##6 và ##8)... làm phần AI Features Sinh module và ràng buộc cộng thêm các phần giúp nó hoạt động đúng và mượt | Tạo model AIInteractionLog lưu log prompt, tạo 2 service inventory_report_service và reorder_suggestion_service gọi Gemini, tạo router gọi service và lưu log db. Thêm test file mock Gemini API call. Cập nhật models/__init__.py và main.py. Cập nhật api_contract.md phần 10. | Chạy pytest cho test_ai_features.py pass 4/4 (mock call). Kiểm tra logic roles_required và bắt lỗi đầy đủ. Bổ sung constraint. | AI (Antigravity) |

| 18/08/2026 | KT2 | Sinh module Stocktakes (Kiểm kê) | Đọc Prompt.md (##6 thiết kế CSDL, ##8 AI), ai_prompt_log.md, api_contract.md (mục 7). Yêu cầu: Sinh 2 model (Stocktake, StocktakeItem), quan hệ với Goods, thêm phân quyền, error codes chuẩn. Sinh models/stocktake.py (Stocktake + StocktakeItem), thêm relationships, cập nhật models/__init__.py. Sinh routers/stocktakes.py (4 endpoint + 1 auto-submit từ StocktakeItem khi nhận quét), thêm full ràng buộc (bắt buộc item ≥1, tổng ≤50, trạng thái), đăng ký trong main.py. Sinh tests/test_stocktakes.py (18 TC đủ ca đúng + ca lỗi/biên + phân quyền). | 1. Cập nhật api_contract.md mục 7 (điền đầy đủ Request/Response/Error codes). 2. Tạo models/stocktake.py. 3. Thêm relationships với Goods. 4. Cập nhật models/__init__.py. 5. Tạo routers/stocktakes.py với 5 endpoints (4 CRUD + 1 auto-submit) + full ràng buộc. 6. Đăng ký blueprint trong main.py. 7. Tạo tests/test_stocktakes.py 18 TC (TC01–TC07 ca đúng, TC08–TC14 ca lỗi/biên, TC15–TC18 phân quyền). | Chạy pytest: **18/18 PASSED** (18.40s). Kiểm chứng: StocktakeItem ≥1 và ≤50, submit từ item tự động set status, rollback đúng, phân quyền 403/401 đúng {error_code, message}. | Sinh viên |

## Ghi chú cách điền

- **Giai đoạn**: KT1 / KT2 / KT3 / Cuối kỳ
- **Prompt (rút gọn)**: tóm tắt nội dung prompt gửi AI (không cần copy nguyên văn)
- **Phản hồi AI**: tóm tắt những gì AI đã làm / sinh ra
- **Kiểm chứng**: bạn đã chạy thử chưa, có sửa gì không, kết quả test pass/fail



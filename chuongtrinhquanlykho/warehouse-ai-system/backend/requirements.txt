# ============================================================
# requirements.txt — Danh sách thư viện Python cho backend
# ============================================================
# Cài đặt: pip install -r requirements.txt
# Khuyến nghị: dùng virtual environment trước khi cài
#   python -m venv venv
#   venv\Scripts\activate   (Windows)
#   source venv/bin/activate (Linux/Mac)
# ============================================================

# --- Web framework ---
Flask==3.0.3                  # Micro web framework chính
Flask-CORS==4.0.1             # Cho phép frontend (React/HTML) gọi API khác origin

# --- Database / ORM ---
Flask-SQLAlchemy==3.1.1       # Tích hợp SQLAlchemy vào Flask
SQLAlchemy==2.0.31            # ORM — ánh xạ class Python ↔ bảng CSDL
alembic==1.13.2               # Migration tool (dùng khi đổi schema)

# --- Authentication ---
Flask-JWT-Extended==4.6.0     # JWT: tạo token, verify, refresh
bcrypt==4.1.3                 # Hash password an toàn (bcrypt algorithm)

# --- Validation ---
marshmallow==3.21.3           # Validate & serialize request/response data
Flask-Marshmallow==1.2.1      # Tích hợp Marshmallow vào Flask

# --- Environment ---
python-dotenv==1.0.1          # Đọc biến từ file .env

# --- AI Integration (dùng ở KT3) ---
google-generativeai==0.7.2    # Gemini API SDK chính thức

# --- Scheduler (dùng ở KT3) ---
APScheduler==3.10.4           # Chạy tác vụ định kỳ (quét tồn kho dưới ngưỡng Min)

# --- Dev / Testing ---
pytest==8.2.2                 # Unit testing framework
pytest-flask==1.3.0           # Helpers test Flask app
